# Server Configuration
PORT=5000
NODE_ENV=production

# MongoDB Connection - replace with your MongoDB Atlas URL
MONGODB_URI=mongodb+srv://username:password@cluster0.example.mongodb.net/farmfolio?retryWrites=true&w=majority

# JWT Secret - use a strong, random string in production
JWT_SECRET=your_secure_jwt_secret_here
